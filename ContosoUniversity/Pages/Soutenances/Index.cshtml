@page
@model ContosoUniversity.Pages.Soutenances.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Soutenance[0].stage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soutenance[0].Turor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Soutenance[0].TitreDeSoutenance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Soutenance) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.stage.StageID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Turor.FirstMidName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitreDeSoutenance)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SoutenanceID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SoutenanceID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SoutenanceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
